# Porta do servidor (Railway define automaticamente em produção)
PORT=3000

# URL do webhook do N8N para receber mensagens do WhatsApp
N8N_WEBHOOK_URL=https://seu-n8n.com/webhook/whatsapp

# Ambiente (production no Railway)
NODE_ENV=production

# Configurações opcionais de reconexão (valores padrão)
# MAX_RECONNECT_ATTEMPTS=5
# BASE_RECONNECT_DELAY=10000